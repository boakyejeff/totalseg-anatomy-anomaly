# Heavy install: TotalSegmentator pulls torch (CPU) and nnU-Net; ~1-2 GB and
# several minutes. Install in an isolated venv, NOT in the repo:
#   python3 -m venv ~/venvs/totalseg && ~/venvs/totalseg/bin/pip install -r requirements.txt
nibabel
numpy
pandas
SimpleITK
TotalSegmentator
scipy
