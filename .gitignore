# Data (real CT scans + produced masks + reports output data)
data/
segs/
logs/
*.nii
*.nii.gz
*.mhd
*.raw
*.dcm

# Virtual environments
venv/
venvs/
.venv/
ENV/

# TotalSegmentator model weights & nnU-Net results
nnUNet_results/
totalsegmentator_weights/
*.pt
*.pth

# Bytecode / caches
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Results: curated small CSV/JSON outputs ARE committed (see below).
# Everything else produced at runtime stays out.
results/*
!results/*.csv
!results/*.json
!results/*.md

# Editors / OS
.DS_Store
.idea/
.vscode/
